{
  "name": "chaos-portal",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx pagefind --site dist --glob '**/*.html'",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "pagefind": "^1.4.0"
  }
}
